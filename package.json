{
	"name": "FNDAR-api",
	"version": "0.1",
	"description": "",
	"scripts": {
		"start": "npx babel src --out-dir dist --ignore node_modules; cd dist && node server.js",
		"watch": "nodemon --ignore dist/ start",
		"build": "./build.sh"
	},
	"license": "MIT",
	"dependencies": {
		"babel-cli": "^6.26.0",
		"babel-preset-env": "^1.6.1"
	}
}
